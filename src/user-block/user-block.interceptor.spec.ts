import { UserBlockInterceptor } from './user-block.interceptor';

describe('UserBlockInterceptor', () => {
  it('should be defined', () => {
    expect(new UserBlockInterceptor()).toBeDefined();
  });
});
